"""pagarmeapisdk.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from pagarmeapisdk.api_helper import APIHelper
from pagarmeapisdk.models.get_address_response import (
    GetAddressResponse,
)
from pagarmeapisdk.models.paging_response import (
    PagingResponse,
)


class ListAddressesResponse(object):
    """Implementation of the 'ListAddressesResponse' model.

    Response object for listing addresses

    Attributes:
        data (List[GetAddressResponse]): The address objects
        paging (PagingResponse): Paging object

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "data": "data",
        "paging": "paging",
    }

    _optionals = [
        "data",
        "paging",
    ]

    _nullables = [
        "data",
        "paging",
    ]

    def __init__(
        self,
        data=APIHelper.SKIP,
        paging=APIHelper.SKIP):
        """Initialize a ListAddressesResponse instance."""
        # Initialize members of the class
        if data is not APIHelper.SKIP:
            self.data = data
        if paging is not APIHelper.SKIP:
            self.paging = paging

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        if "data" in dictionary.keys():
            data = [
                GetAddressResponse.from_dictionary(x)
                for x in dictionary.get("data")
            ] if dictionary.get("data") else None
        else:
            data = APIHelper.SKIP
        if "paging" in dictionary.keys():
            paging =\
                PagingResponse.from_dictionary(
                dictionary.get("paging"))\
                if dictionary.get("paging") else None
        else:
            paging = APIHelper.SKIP

        # Return an object of this model
        return cls(data,
                   paging)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _data=(
            self.data
            if hasattr(self, "data")
            else None
        )
        _paging=(
            self.paging
            if hasattr(self, "paging")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"data={_data!r}, "
            f"paging={_paging!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _data=(
            self.data
            if hasattr(self, "data")
            else None
        )
        _paging=(
            self.paging
            if hasattr(self, "paging")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"data={_data!s}, "
            f"paging={_paging!s}, "
            f")"
        )
