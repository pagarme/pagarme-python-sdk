"""pagarmeapisdk.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from pagarmeapisdk.api_helper import APIHelper
from pagarmeapisdk.models.get_recipient_response import (
    GetRecipientResponse,
)


class GetBalanceResponse(object):
    """Implementation of the 'GetBalanceResponse' model.

    Balance

    Attributes:
        currency (str): Currency (official ISO 4217 currency names)
        available_amount (int): Amount available for transferring in cents
        recipient (GetRecipientResponse): Recipient
        transferred_amount (int): Amount transfered in cents
        waiting_funds_amount (int): Amount waiting in cents
        payment_profile_id (str): Operational id of merchant in payments operations
            (new)

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "payment_profile_id": "payment_profile_id",
        "currency": "currency",
        "available_amount": "available_amount",
        "recipient": "recipient",
        "transferred_amount": "transferred_amount",
        "waiting_funds_amount": "waiting_funds_amount",
    }

    _optionals = [
        "currency",
        "available_amount",
        "recipient",
        "transferred_amount",
        "waiting_funds_amount",
    ]

    _nullables = [
        "currency",
        "available_amount",
        "recipient",
        "transferred_amount",
        "waiting_funds_amount",
        "payment_profile_id",
    ]

    def __init__(
        self,
        payment_profile_id=None,
        currency=APIHelper.SKIP,
        available_amount=APIHelper.SKIP,
        recipient=APIHelper.SKIP,
        transferred_amount=APIHelper.SKIP,
        waiting_funds_amount=APIHelper.SKIP):
        """Initialize a GetBalanceResponse instance."""
        # Initialize members of the class
        if currency is not APIHelper.SKIP:
            self.currency = currency
        if available_amount is not APIHelper.SKIP:
            self.available_amount = available_amount
        if recipient is not APIHelper.SKIP:
            self.recipient = recipient
        if transferred_amount is not APIHelper.SKIP:
            self.transferred_amount = transferred_amount
        if waiting_funds_amount is not APIHelper.SKIP:
            self.waiting_funds_amount = waiting_funds_amount
        self.payment_profile_id = payment_profile_id

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        payment_profile_id =\
            dictionary.get("payment_profile_id")\
            if dictionary.get("payment_profile_id")\
                else None
        currency =\
            dictionary.get("currency")\
            if "currency" in dictionary.keys()\
                else APIHelper.SKIP
        available_amount =\
            dictionary.get("available_amount")\
            if "available_amount" in dictionary.keys()\
                else APIHelper.SKIP
        if "recipient" in dictionary.keys():
            recipient =\
                GetRecipientResponse.from_dictionary(
                dictionary.get("recipient"))\
                if dictionary.get("recipient") else None
        else:
            recipient = APIHelper.SKIP
        transferred_amount =\
            dictionary.get("transferred_amount")\
            if "transferred_amount" in dictionary.keys()\
                else APIHelper.SKIP
        waiting_funds_amount =\
            dictionary.get("waiting_funds_amount")\
            if "waiting_funds_amount" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(payment_profile_id,
                   currency,
                   available_amount,
                   recipient,
                   transferred_amount,
                   waiting_funds_amount)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _currency=(
            self.currency
            if hasattr(self, "currency")
            else None
        )
        _available_amount=(
            self.available_amount
            if hasattr(self, "available_amount")
            else None
        )
        _recipient=(
            self.recipient
            if hasattr(self, "recipient")
            else None
        )
        _transferred_amount=(
            self.transferred_amount
            if hasattr(self, "transferred_amount")
            else None
        )
        _waiting_funds_amount=(
            self.waiting_funds_amount
            if hasattr(self, "waiting_funds_amount")
            else None
        )
        _payment_profile_id=self.payment_profile_id
        return (
            f"{self.__class__.__name__}("
            f"currency={_currency!r}, "
            f"available_amount={_available_amount!r}, "
            f"recipient={_recipient!r}, "
            f"transferred_amount={_transferred_amount!r}, "
            f"waiting_funds_amount={_waiting_funds_amount!r}, "
            f"payment_profile_id={_payment_profile_id!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _currency=(
            self.currency
            if hasattr(self, "currency")
            else None
        )
        _available_amount=(
            self.available_amount
            if hasattr(self, "available_amount")
            else None
        )
        _recipient=(
            self.recipient
            if hasattr(self, "recipient")
            else None
        )
        _transferred_amount=(
            self.transferred_amount
            if hasattr(self, "transferred_amount")
            else None
        )
        _waiting_funds_amount=(
            self.waiting_funds_amount
            if hasattr(self, "waiting_funds_amount")
            else None
        )
        _payment_profile_id=self.payment_profile_id
        return (
            f"{self.__class__.__name__}("
            f"currency={_currency!s}, "
            f"available_amount={_available_amount!s}, "
            f"recipient={_recipient!s}, "
            f"transferred_amount={_transferred_amount!s}, "
            f"waiting_funds_amount={_waiting_funds_amount!s}, "
            f"payment_profile_id={_payment_profile_id!s}, "
            f")"
        )
