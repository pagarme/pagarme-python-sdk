"""pagarmeapisdk.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from pagarmeapisdk.api_helper import APIHelper
from pagarmeapisdk.models.create_fine_request import (
    CreateFineRequest,
)
from pagarmeapisdk.models.create_interest_request import (
    CreateInterestRequest,
)


class CreateSubscriptionBoletoRequest(object):
    """Implementation of the 'CreateSubscriptionBoletoRequest' model.

    Information about fines and interest on the "boleto" used from payment

    Attributes:
        interest (CreateInterestRequest): The model property of type
            CreateInterestRequest.
        fine (CreateFineRequest): The model property of type CreateFineRequest.
        max_days_to_pay_past_due (int): The model property of type int.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "interest": "interest",
        "fine": "fine",
        "max_days_to_pay_past_due": "max_days_to_pay_past_due",
    }

    _optionals = [
        "interest",
        "fine",
        "max_days_to_pay_past_due",
    ]

    _nullables = [
        "max_days_to_pay_past_due",
    ]

    def __init__(
        self,
        interest=APIHelper.SKIP,
        fine=APIHelper.SKIP,
        max_days_to_pay_past_due=APIHelper.SKIP):
        """Initialize a CreateSubscriptionBoletoRequest instance."""
        # Initialize members of the class
        if interest is not APIHelper.SKIP:
            self.interest = interest
        if fine is not APIHelper.SKIP:
            self.fine = fine
        if max_days_to_pay_past_due is not APIHelper.SKIP:
            self.max_days_to_pay_past_due = max_days_to_pay_past_due

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        interest =\
            CreateInterestRequest.from_dictionary(
                dictionary.get("interest"))\
                if "interest" in dictionary.keys()\
                else APIHelper.SKIP
        fine =\
            CreateFineRequest.from_dictionary(
                dictionary.get("fine"))\
                if "fine" in dictionary.keys()\
                else APIHelper.SKIP
        max_days_to_pay_past_due =\
            dictionary.get("max_days_to_pay_past_due")\
            if "max_days_to_pay_past_due" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(interest,
                   fine,
                   max_days_to_pay_past_due)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _interest=(
            self.interest
            if hasattr(self, "interest")
            else None
        )
        _fine=(
            self.fine
            if hasattr(self, "fine")
            else None
        )
        _max_days_to_pay_past_due=(
            self.max_days_to_pay_past_due
            if hasattr(self, "max_days_to_pay_past_due")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"interest={_interest!r}, "
            f"fine={_fine!r}, "
            f"max_days_to_pay_past_due={_max_days_to_pay_past_due!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _interest=(
            self.interest
            if hasattr(self, "interest")
            else None
        )
        _fine=(
            self.fine
            if hasattr(self, "fine")
            else None
        )
        _max_days_to_pay_past_due=(
            self.max_days_to_pay_past_due
            if hasattr(self, "max_days_to_pay_past_due")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"interest={_interest!s}, "
            f"fine={_fine!s}, "
            f"max_days_to_pay_past_due={_max_days_to_pay_past_due!s}, "
            f")"
        )
