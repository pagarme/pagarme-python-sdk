"""pagarmeapisdk.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from pagarmeapisdk.api_helper import APIHelper


class GetTransferSettingsResponse(object):
    """Implementation of the 'GetTransferSettingsResponse' model.

    Attributes:
        transfer_enabled (bool): The model property of type bool.
        transfer_interval (str): The model property of type str.
        transfer_day (int): The model property of type int.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "transfer_enabled": "transfer_enabled",
        "transfer_interval": "transfer_interval",
        "transfer_day": "transfer_day",
    }

    _optionals = [
        "transfer_enabled",
        "transfer_interval",
        "transfer_day",
    ]

    _nullables = [
        "transfer_enabled",
        "transfer_interval",
        "transfer_day",
    ]

    def __init__(
        self,
        transfer_enabled=APIHelper.SKIP,
        transfer_interval=APIHelper.SKIP,
        transfer_day=APIHelper.SKIP):
        """Initialize a GetTransferSettingsResponse instance."""
        # Initialize members of the class
        if transfer_enabled is not APIHelper.SKIP:
            self.transfer_enabled = transfer_enabled
        if transfer_interval is not APIHelper.SKIP:
            self.transfer_interval = transfer_interval
        if transfer_day is not APIHelper.SKIP:
            self.transfer_day = transfer_day

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        transfer_enabled =\
            dictionary.get("transfer_enabled")\
            if "transfer_enabled" in dictionary.keys()\
                else APIHelper.SKIP
        transfer_interval =\
            dictionary.get("transfer_interval")\
            if "transfer_interval" in dictionary.keys()\
                else APIHelper.SKIP
        transfer_day =\
            dictionary.get("transfer_day")\
            if "transfer_day" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(transfer_enabled,
                   transfer_interval,
                   transfer_day)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _transfer_enabled=(
            self.transfer_enabled
            if hasattr(self, "transfer_enabled")
            else None
        )
        _transfer_interval=(
            self.transfer_interval
            if hasattr(self, "transfer_interval")
            else None
        )
        _transfer_day=(
            self.transfer_day
            if hasattr(self, "transfer_day")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"transfer_enabled={_transfer_enabled!r}, "
            f"transfer_interval={_transfer_interval!r}, "
            f"transfer_day={_transfer_day!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _transfer_enabled=(
            self.transfer_enabled
            if hasattr(self, "transfer_enabled")
            else None
        )
        _transfer_interval=(
            self.transfer_interval
            if hasattr(self, "transfer_interval")
            else None
        )
        _transfer_day=(
            self.transfer_day
            if hasattr(self, "transfer_day")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"transfer_enabled={_transfer_enabled!s}, "
            f"transfer_interval={_transfer_interval!s}, "
            f"transfer_day={_transfer_day!s}, "
            f")"
        )
